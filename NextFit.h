
/*next-fit algorithm*/
void Sim_Next_Fit(struct process_list* plist, FILE* output)
{
    unsigned int sim_time = 0;
    
    printf("time %dms: Simulator started (Contiguous -- Next-Fit)\n", sim_time);
    
    
    
    
    
    
    printf("time %dms: Simulator ended (Contiguous -- Next-Fit)\n", sim_time);
}