
/*worst-fit algorithm*/
void Sim_Worst_Fit(struct process_list* plist, FILE* output)
{
    unsigned int sim_time = 0;
    
    printf("time %dms: Simulator started (Contiguous -- Worst-Fit)\n", sim_time);
    
    
    
    
    
    
    printf("time %dms: Simulator ended (Contiguous -- Worst-Fit)\n", sim_time);
}